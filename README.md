# virtual-mouse

# Virtual Mouse using Hand Tracking 🖐️🖱️

This project uses *Python + OpenCV + MediaPipe + PyAutoGUI* to control the mouse cursor using only your hand via the camera.

The cursor is moved by the middle finger, and a click is executed when the thumb approaches the index finger.

## ⚙️ Features: - Mouse movement using hand motion without touching.

- Automatic click when the thumb approaches the index finger.

- Uses the MediaPipe library for high-precision hand tracking.

## 🚀 How to install:

1. Install the required libraries:

```bash

pip install opencv-python mediapipe pyautogui
